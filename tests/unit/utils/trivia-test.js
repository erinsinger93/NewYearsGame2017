import trivia from 'new-years-game/utils/trivia';
import { module, test } from 'qunit';

module('Unit | Utility | trivia');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trivia();
  assert.ok(result);
});
